# 16. Leer 15 números negativos y convertirlos a positivos e imprimir dichos números.

for i in range(15):
    num = int(input("Ingrese un número negativo: "))
    if num < 0:
        num = abs(num)
    print(num)
